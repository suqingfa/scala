package org.example.obj

/**
 * import 用法
 * <li> 在文件顶层使用 import, 当前文件的类都可以使用
 * <li> 局部导入，在需要的地方 import
 * <li> 通配符导入 import java.util._
 * <li> 给类命名 import java.util.{ArrayList => JAL}
 * <li> 导入多个类 import java.util.{HashSet, ArrayList}
 * <li> 屏蔽类 import java.util.{ArrayList => _}
 * <li> 导入包的绝对路径 import _root_.java.util.Set
 *
 * <p>
 * scala默认导入包
 * <li> import java.lang._
 * <li> import scala._
 * <li> import scala.Predef._
 */
class ScalaImport {
}
